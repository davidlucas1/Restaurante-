<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainLayout"
    android:layout_width="75dp"
    android:layout_height="110dp"
    android:layout_margin="4dp"
    android:layout_marginTop="4dp"
    android:background="@drawable/backgraund_categoria"
    android:elevation="3dp">

    <ImageView
        android:id="@+id/categoriaImage"
        android:layout_width="36dp"
        android:layout_height="35dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.487"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.213"
        app:srcCompat="@drawable/cat_1" />

    <TextView
        android:id="@+id/categoriaNome"
        android:layout_width="58dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:gravity="center"
        android:text="TextView"
        app:layout_constraintEnd_toEndOf="@+id/categoriaImage"
        app:layout_constraintStart_toStartOf="@+id/categoriaImage"
        app:layout_constraintTop_toBottomOf="@+id/categoriaImage" />
</androidx.constraintlayout.widget.ConstraintLayout>